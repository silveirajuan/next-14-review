export default function OnboardingPage() {
  return (
    <main className="w-full h-screen flex items-center justify-center">
      Hello, <span className="ml-1 text-purple-500">{` {{ name }}`} 👋</span>
    </main>
  )
}